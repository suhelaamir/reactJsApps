import React, { Component, Fragment } from 'react'

class Dashboard extends Component {
    render() {
        return(
            <Fragment>
                dashboard component
            </Fragment>
        );
    }
}

export default Dashboard;